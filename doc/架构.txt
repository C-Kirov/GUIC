GUIC_2.0/
├── src/                              # 源代码主目录
│   ├── main.cpp                      # 程序入口 (WinMain)，Winsock 进程级初始化，窗口创建与消息循环
│   ├── main.h                        # 主窗口过程声明 + 自定义消息 (WM_WEATHER_READY / WM_TRAY_ICON) + WM_DPICHANGED 兼容宏
│   ├── windowproc.cpp                # 主窗口过程函数：菜单（国际化）、定时器、天气线程回调、服务器启停、双缓冲绘制
│   ├── i18n.h / i18n.cpp             # 国际化：StringID 枚举 + 中/英字符串数组，运行时随语言切换
│   ├── resource.h                    # 资源 ID 定义（菜单/对话框/控件 ID，含静态文本唯一 ID）
│   ├── resource.rc                   # 资源脚本（图标、manifest、所有对话框模板，中文为默认回退，运行时覆写英文）
│   ├── config/special_days.h         # 空头文件（内容已合并至 config.h，保留以兼容构建配置）
│   │
│   ├── config/
│   │   ├── config.h                  # 全局配置类 AppConfig 声明（单例、线程安全）
│   │   └── config.cpp                # 配置实现（INI 读写、特殊日管理、网络时间同步 EWMA）
│   │
│   ├── utils/
│   │   ├── utils.h / utils.cpp       # 工具函数（路径处理、ReadWorkFile）
│   │   ├── logger.h / logger.cpp     # 日志系统（WriteLog、系统信息记录、LogWin32Error）
│   │   ├── network.h / network.cpp   # SNTPv4 客户端（RFC 5905 标准报文）+ Marzullo 多服务器共识 + Winsock 进程级初始化
│   │   ├── lunar_online.h / .cpp     # 在线农历（HKO 官方公历/农历对照表，HTTPS 拉取，离线本地兜底）
│   │   ├── lunar.h / lunar.cpp       # 公历??农历转换（1900-2100 数据表）、天干地支/生肖
│   │   ├── weather.h / weather.cpp   # wttr.in 天气客户端（UTF-8→ANSI，lang 随语言）
│   │   ├── http_server.h / .cpp      # Web 控制面板（TCP 8080，HTML 页面国际化，/set 可切换语言）
│   │   ├── ntp_server.h / .cpp       # 局域网 NTP 服务器（UDP 123，stratum 2，RFC 5905 标准应答，后台线程）
│   │   ├── renderer.h / renderer.cpp # 主窗口双缓冲绘制引擎（时钟/倒计时/消息/天气/work.txt）
│   │   └── gdi_utils.h               # GDI 资源 RAII 封装（ScopedPen/Brush/Font/DC/Bitmap）
│   │
│   ├── dialogs/
│   │   ├── dialog_message.h / .cpp   # 自定义显示消息设置对话框
│   │   ├── dialog_countdown.h /.cpp  # 倒计时设置对话框（一次性 + 每日）
│   │   ├── dialog_settings.h / .cpp  # 窗口/字体/颜色/天气城市/NTP服务器/服务器端口设置/关于 对话框
│   │   └── dialog_special.h / .cpp   # 特殊日期/世界时钟/模拟时钟对话框
│   │
│   └── GUIC_2.0.ico                  # 程序图标
│
├── doc/
│   └── 架构.txt                      # 本架构说明文档
│
├── build/                            # Code::Blocks 编译输出目录（config.ini/special_days.txt/log.txt/work.txt 为运行时数据）
└── CMakeLists.txt                    # CMake 构建脚本（与 Code::Blocks 等效，含全部源文件）
└── GUIC_2.0.cbp                      # Code::Blocks 项目文件

编码约定：所有源文件为 GBK/GB18030 编码（非 UTF-8），修改时注意编码。

构建方式：
  1) Code::Blocks：打开 GUIC_2.0.cbp，选择 GCC MinGW64 编译器构建，输出 build/GUIC_2.exe
  2) CMake：cmake -G "MinGW Makefiles" -S . -B cmake-build && cmake --build cmake-build
     产物统一输出到 build/GUIC_2.exe（RUNTIME_OUTPUT_DIRECTORY 指向 build/）；
     注：CMakeLists 已包含 windres 的 -I src/ 参数以定位 .ico/.manifest

运行数据（与 exe 同目录）：
  config.ini        显示/字体/颜色/倒计时/服务器 等配置
  special_days.txt  特殊日期（名称|月|日|时|分|每年|年|节气）
  work.txt          外部文本，修改时间变化时自动重绘显示
  log.txt           运行日志

Winsock 生命周期（修复后）：
  WinMain 启动时 WinsockInit()（进程级引用计数），退出时 WinsockShutdown()；
  GetNetworkTime/FetchWeather 仅防御性调用 WinsockInit()，不再各自 WSAStartup/WSACleanup；
  HTTP/NTP 服务器线程因此不再出现 WSANOTINITIALISED(10093) 错误。


#ifndef MAIN_H
#define MAIN_H

#include <windows.h>
#include "resource.h"   // 所有控件/菜单/对话框 ID

// 主窗口过程声明
LRESULT CALLBACK WindowProc(HWND hwnd, UINT uMsg, WPARAM wParam, LPARAM lParam);

#endif // MAIN_H
